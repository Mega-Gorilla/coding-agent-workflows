# Startup Status

## On Startup
Issue-first repo. Accurately understand current progress using `gh`:

1. **Issues/PRs**: `gh issue status`, `gh issue list --state open --limit 20`, `gh pr status`, `gh pr list --state all --limit 20`
2. **GitHub history**: `git log -5 --oneline`, `gh run list --limit 5`
3. **Additional context if needed**: Related docs, repo conventions (`rg` for search)
4. **Display progress in Japanese**: Summarize current state, in-flight work, blockers (no file creation)
